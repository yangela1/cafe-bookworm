// Canonical origin. Used for metadataBase, the sitemap, and robots.txt, so
// there's one place to change if the domain ever moves.
export const SITE_URL = 'https://www.cafebookworm.com'

// Lowercase by design — the brand is styled lowercase throughout the UI, and
// page titles are rendered verbatim in search results, so they match it.
export const SITE_NAME = 'cafe bookworm'
